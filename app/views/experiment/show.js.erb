<% EXPERIMENTS.each do |experiment_name| %>
  $("<%= "#panelsStayOpen-collapse-#{experiment_name} .result" %>").html("<%= j line_chart @results[experiment_name][:result], discrete: true, points: false,  suffix: "ms", xtitle:  @results[experiment_name][:x_title], height: '400px', width: '800px', colors: @colors, library: {backgroundColor: 'red'}%>");
<%end %>
